/**
 * `@domain` — verdad única de estados, roles y formateo del dominio ShipGo.
 * Alineado a `planning/CONTRACTS.md` §1 (estados) y §3 (roles).
 */

export * from './estados';
export * from './roles';
export * from './format';
