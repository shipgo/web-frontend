/**
 * Feature `landing` — página pública de presentación de ShipGo (`SHG-FE-044`).
 * Sirve en `/` para visitantes sin sesión (ver `RootRoute` en
 * `app/routes/index.jsx`).
 */

export { default as LandingPage } from './pages/Landing';
